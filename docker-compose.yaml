version: '3.8'

services:
  my_web:
    image: nginx:alpine
    container_name: my_web
    ports:
      - "8080:80"         # Host port 8080 → container port 80
    volumes:
      - ./html:/usr/share/nginx/html   # Persistent volume for your website files
    restart: always
